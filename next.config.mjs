/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["gbo1qdj0roz2nqut.public.blob.vercel-storage.com"],
  },
};

export default nextConfig;
